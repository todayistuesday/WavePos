# WavePos 작업 지침

## 기본 응답
- 항상 한국어로 답변한다.
- 기술 용어, API 필드명, 라이브러리명은 영어를 허용한다.
- 설명은 간결하게 하되, 변경 요약과 테스트 여부는 반드시 포함한다.

## 작업 원칙
- 작업 전 `AGENTS.md`, `.agents/rules/*`, 관련 `docs/prd/` 문서를 먼저 확인한다.
- 사용자가 지정한 타겟 외 기존 UI/UX, 레이아웃, CSS, 컴포넌트는 수정하지 않는다.
- 코드 수정 완료 답변은 반드시 `[검토 결과: UI/UX 영향 없음]`으로 시작한다.
- 전체 파일 내용을 출력하지 않고, 변경된 블록과 핵심만 설명한다.

## 프로젝트 구조
- `App.tsx`, `index.tsx`, `index.css`: 앱 진입점 및 전역 레이아웃
- `components/`: 공통 UI 컴포넌트
- `pages/`: 도메인별 페이지
- `services/`: 데이터 접근 및 저장 로직
- `utils/`: 순수 유틸리티
- `.agents/rules/`: 에이전트 공통 규칙
- `.agents/skills/`: 반복 작업용 스킬 문서
- `docs/prd/`: 기능 PRD 문서

## Git
- 커밋은 사용자가 명시적으로 요청한 경우에만 수행한다.
- `git add .`는 절대 사용하지 않고, 파일별로 명시적으로 스테이징한다.
